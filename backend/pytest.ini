[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: requires a live dependency (PostgreSQL, Ollama) to be running
